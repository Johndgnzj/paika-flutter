# ✅ Paika 部署檢查清單

## 首次部署（需老闆操作）

### 1. Firebase 專案建立
- [ ] 前往 https://console.firebase.google.com/
- [ ] 建立專案，名稱：**paika**
- [ ] 專案 ID 設為：**paika**（或可用的類似名稱）
- [ ] 啟用 Firebase Hosting

### 2. Firebase CLI 登入
```bash
firebase login
```
- [ ] 瀏覽器會自動開啟
- [ ] 使用 Google 帳號登入
- [ ] 授權 Firebase CLI

### 3. 連結專案
```bash
cd ~/Documents/vibe_projects/paika
firebase use --add
```
- [ ] 選擇剛建立的 paika 專案
- [ ] 設為預設專案

### 4. 首次部署
```bash
./scripts/deploy.sh
```
- [ ] 等待編譯完成（約 20-30 秒）
- [ ] 確認部署成功
- [ ] 記錄部署網址（例如：https://paika.web.app）

## 後續更新（Neo 可自動執行）

### 自動化更新流程
```bash
cd ~/Documents/vibe_projects/paika
./scripts/deploy.sh
```

### 手動更新步驟
1. [ ] 修改程式碼
2. [ ] 測試功能
3. [ ] 編譯：`flutter build web --release`
4. [ ] 部署：`firebase deploy --only hosting`

## 驗證清單

### 部署後檢查
- [ ] 網站可正常訪問
- [ ] 首頁載入正常
- [ ] 開始新局功能正常
- [ ] 記分功能正常
- [ ] 電子骰子功能正常
- [ ] 本地儲存功能正常
- [ ] 深色/淺色模式切換正常

### 效能檢查
- [ ] 首次載入時間 < 3 秒
- [ ] 互動流暢無卡頓
- [ ] 手機版顯示正常

## 🔐 權限設定

### Firebase 專案權限
如果要讓 Neo 自動部署，需要：

1. **選項 A：使用 Firebase Token（推薦）**
   ```bash
   firebase login:ci
   ```
   將產生的 token 提供給 Neo

2. **選項 B：使用 Service Account**
   - 在 Firebase Console 下載 service account key
   - 設定環境變數

3. **選項 C：手動部署**
   - 老闆保留登入權限
   - Neo 只編譯，老闆手動 deploy

## 📊 監控設定

### Firebase Console
- [ ] 設定通知：部署成功/失敗
- [ ] 啟用 Analytics（選用）
- [ ] 設定用量警告

## 🌐 自訂網域（選用）

如果要使用自己的網域：
- [ ] 購買網域（例如：paika.app）
- [ ] 在 Firebase Console 設定自訂網域
- [ ] 更新 DNS 記錄
- [ ] 等待驗證完成（幾小時到 1 天）

## 🔄 回滾計畫

如果新版本有問題：
```bash
# 查看歷史版本
firebase hosting:channel:list

# 回滾到上一版本
firebase hosting:clone paika:live paika:live --only-live
```

## 📝 完成狀態

- [ ] Firebase 專案已建立
- [ ] Firebase CLI 已登入
- [ ] 專案已連結
- [ ] 首次部署成功
- [ ] 網址已記錄
- [ ] Neo 已獲得部署權限（選用）

---

**當前專案資訊**
- 專案名稱：Paika（牌咖）
- 專案路徑：`~/Documents/vibe_projects/paika/`
- 編譯產物：`build/web/`
- 部署腳本：`./scripts/deploy.sh`

**第一次部署請依照順序執行，完成後 Neo 可接手後續更新！** 🚀
